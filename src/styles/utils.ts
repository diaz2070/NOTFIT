// Primary color: #531DA5 (converted to rgba for glow effect)
const glowColor = 'rgba(83, 29, 165, 0.35)';

const shadow = `0 0 5px ${glowColor}, 0 0 10px ${glowColor}, 0 0 15px ${glowColor}, 0 0 20px ${glowColor}`;
export default shadow;
